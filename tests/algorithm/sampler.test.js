/** Information gain computation correctness tests. */
import { describe, it, expect } from 'vitest';

describe('Sampler', () => {
  it.todo('selectNext returns highest-EIG question');
  it.todo('respects viewport restriction (FR-016)');
  it.todo('does not permanently exclude cells (FR-019)');
});
