/** SC-008: Mobile/tablet layout and cross-browser visual test. */
import { test } from '@playwright/test';

test.describe('Responsive Layout (SC-008)', () => {
  test.fixme('mobile viewport 375x667 is usable');
  test.fixme('tablet viewport 768x1024 is usable');
  test.fixme('desktop viewport 1280x800 is usable');
  test.fixme('cross-browser: Chromium, Firefox, WebKit render identically');
});
