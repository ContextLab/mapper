/** US1: Domain select → answer → heatmap update visual test. */
import { test } from '@playwright/test';

test.describe('Quiz Flow (US1)', () => {
  test.fixme('captures map view screenshot');
  test.fixme('captures quiz active screenshot');
  test.fixme('captures results panel screenshot');
  test.fixme('captures about modal screenshot');
});
