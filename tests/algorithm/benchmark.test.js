/** SC-011: Active learning vs random baseline benchmark. */
import { describe, bench } from 'vitest';

describe('Active Learning Benchmark (SC-011)', () => {
  bench.todo('GP+EIG achieves lower MAE than random over 50-question session');
});
